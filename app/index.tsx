import { Text, View } from "react-native";

export default function Landing() {
  return (
    <View className="flex-1 items-center justify-center bg-primary">
      <Text className="text-defaulttext text-x2">Pickup</Text>
    </View>
  );
}
