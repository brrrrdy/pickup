{
  "name": "pickup",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "api:start": "node api/server.js",
    "api:start:local": "node -r dotenv/config api/server.js dotenv_config_path=.env.local",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "EXPO_NO_DEPENDENCY_VALIDATION=1 expo start --web",
    "test": "jest",
    "test:watch": "jest --watch",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d postgres",
    "db:migrate": "cat db/migrations/*.sql | docker compose exec -T postgres psql -U pickup -d pickup",
    "db:seed": "cat db/seeds/*.sql | docker compose exec -T postgres psql -U pickup -d pickup",
    "db:test:up": "docker compose up -d postgres_test",
    "db:test:down": "docker compose stop postgres_test",
    "db:test:reset": "npm run db:test:up && docker compose exec -T postgres_test psql -U pickup -d pickup_test -c \"DROP SCHEMA IF EXISTS public CASCADE; CREATE SCHEMA public;\"",
    "db:test:migrate": "cat db/migrations/*.sql | docker compose exec -T postgres_test psql -U pickup -d pickup_test",
    "db:test:seed": "cat db/seeds/*.sql | docker compose exec -T postgres_test psql -U pickup -d pickup_test",
    "db:test:prepare": "npm run db:test:reset && npm run db:test:migrate && npm run db:test:seed"
  },
  "dependencies": {
    "@react-native-masked-view/masked-view": "^0.3.2",
    "@react-native-picker/picker": "^2.11.4",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "expo": "~54.0.33",
    "expo-linear-gradient": "^56.0.4",
    "expo-status-bar": "~3.0.9",
    "express": "^5.2.1",
    "nativewind": "^4.2.4",
    "pg": "^8.21.0",
    "react": "19.1.0",
    "react-date-picker": "^12.0.2",
    "react-dom": "19.1.0",
    "react-native": "0.81.5",
    "react-native-calendars": "^1.1314.0",
    "react-native-reanimated": "~4.1.1",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-web": "^0.21.0"
  },
  "devDependencies": {
    "@types/react": "~19.1.0",
    "babel-plugin-module-resolver": "^5.0.3",
    "babel-preset-expo": "^56.0.13",
    "jest": "^29.7.0",
    "prettier-plugin-tailwindcss": "^0.5.14",
    "supertest": "^7.2.2",
    "tailwindcss": "^3.4.19",
    "typescript": "~5.9.2"
  },
  "private": true
}
